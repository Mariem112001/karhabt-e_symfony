<form method="post" action="{{ path('app_reponse_reclamation_delete', {'idreponser': reponse_reclamation.idreponser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse_reclamation.idreponser) }}">
    <button class="btn btn-danger" style="font-size: 1rem;">Supprimer</button>
</form>
