<form method="post" action="{{ path('app_reclamation_delete', {'idr': reclamation.idr}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.idr) }}">
    <div class="mt-3"> <!-- Suppression de la classe text-center pour empêcher le centrage du bouton -->
        <button type="submit" class="btn btn-outline-danger" style="margin-left: 10px;">Supprimer</button>
    </div>
</form>
