{% extends 'baseF.html.twig' %}

{% block title %}Modifier Dossier des pappiers{% endblock %}

{% block body %}
    <h1 class="h3 mb-5 text-white">Modifier vos papier</h1>

    {{ include('demande_dossier/_form.html.twig', {'button_label': 'Update'}) }}

    

    {{ include('demande_dossier/_delete_form.html.twig') }}


    <a  class="btn btn-primary" href="{{ path('app_demande_dossier_index') }}">back to list</a>

{% endblock %}
