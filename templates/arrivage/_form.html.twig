 
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8" align="center">
                {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }} 
                <div class="form-group">
                    {{ form_label(form.quantite, 'Quantité', {'label_attr': {'class': 'form-label', 'style': 'color: #4F79C7;' }}) }}
                    {{ form_widget(form.quantite, {'attr': {'class': 'form-control', 'style': 'width: 50%;'}}) }}
                     <div class="error-message">{{ form_errors(form.quantite) }}</div>
                </div>
                <div class="form-group">
                    {{ form_label(form.dateentree, 'Date d\entree', {'label_attr': {'class': 'form-label', 'style': 'color: #4F79C7;' }}) }}
                    {{ form_widget(form.dateentree, {'attr': {'class': 'form-control', 'style': 'width: 50%;'}}) }}
                     {{ form_errors(form.dateentree) }}
                </div>
                <div class="form-group">
                    {{ form_label(form.voiture, 'Voiture', {'label_attr': {'class': 'form-label', 'style': 'color: #4F79C7;' }}) }}
                    {{ form_widget(form.voiture, {'attr': {'class': 'form-control', 'style': 'width: 50%;'}}) }}
                     {{ form_errors(form.voiture) }}
                </div>
                <div class="card-footer">
                    <button type="submit" class="btn btn-primary">Ajouter</button>
                    <a href="{{ path('app_arrivage_index') }}" class="btn btn-secondary">Retour</a>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
 