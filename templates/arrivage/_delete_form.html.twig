<form method="post" action="{{ path('app_arrivage_delete', {'ida': arrivage.ida}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arrivage.ida) }}">
    <button class="btn">Delete</button>
</form>
