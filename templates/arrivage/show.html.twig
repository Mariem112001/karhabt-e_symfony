<!DOCTYPE html>

<title>Arrivage</title>

{% block body %}
    <h1>Arrivage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ida</th>
                <td>{{ arrivage.ida }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ arrivage.quantite }}</td>
            </tr>
            <tr>
                <th>Dateentree</th>
                <td>{{ arrivage.dateentree ? arrivage.dateentree|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_arrivage_index') }}">back to list</a>

    <a href="{{ path('app_arrivage_edit', {'ida': arrivage.ida}) }}">edit</a>

    {{ include('arrivage/_delete_form.html.twig') }}
{% endblock %}
